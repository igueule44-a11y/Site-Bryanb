name: Ping Google sitemap

on:
  push:
    branches: [main]

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Google of sitemap update
        run: curl -sf "https://www.google.com/ping?sitemap=https://omnyx.agency/sitemap.xml"
